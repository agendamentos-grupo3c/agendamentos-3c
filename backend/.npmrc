# O Render builda a partir do código-fonte (tsc), então o build precisa das
# devDependencies (typescript, @types/node, @types/pg). Como o Render roda com
# NODE_ENV=production, o `npm ci` omitiria as devDeps; este flag força a instalação.
include=dev
